<!DOCTYPE html>
<html>

  <head>
    <% include ../../components/header.ejs %>
  </head>

  <body>

    <% include ../../components/navbar.ejs %>
    <% data.forEach(function(videoObject) { %>
    <iframe id="ytplayer" width="35%" height="200px" src="https://www.youtube-nocookie.com/embed/<%= videoObject.videoID %>?" frameborder="0" allow="autoplay; encrypted-media" allowfullscreen></iframe>
    <% }); %>

</html>
